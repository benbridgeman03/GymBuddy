import 'package:flutter/material.dart';

class WorkoutExerciseEditor extends StatefulWidget {
  final String exerciseId;

  const WorkoutExerciseEditor({super.key, required this.exerciseId});

  @override
  State<WorkoutExerciseEditor> createState() => _WorkoutExerciseEditor();
}

class _WorkoutExerciseEditor extends State<WorkoutExerciseEditor> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
